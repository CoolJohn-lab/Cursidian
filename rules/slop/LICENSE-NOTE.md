# Slop rules attribution

Typography chars in `builtin-typography.json` and packs under `packs/` were snapshotted from [`llm-slop-detector`](https://github.com/mandakan/llm-slop-detector) (MIT License) for Cursidian's first-party engine. They are vendored data, not a runtime dependency.
